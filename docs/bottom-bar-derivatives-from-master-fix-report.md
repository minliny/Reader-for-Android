# Bottom Bar Derivatives From Master Fix Report

## 1. 总体结论

BOTTOM_BAR_DERIVATIVES_FROM_MASTER_FIX_NOT_READY

本轮已按要求以控制层母版为唯一公共层来源，对 4 个现有底栏功能派生 screen 发起直接编辑修复。第一次 Stitch 编辑完成并写入了部分改动；第二次针对残留 P0 的精修请求超时，轮询后未确认产生新的 HTML 结果。

自检显示：4 个 screen 的公共层仍未完全对齐母版，尤其是朗读、界面、设置 screen 仍存在底部控制层规格不一致、图标不一致、章节进度不一致、朗读展开区旧结构残留等问题。因此当前不建议进入只读复审。

## 2. 使用母版

| 母版 screen 名称 | Screen ID | 是否修改 |
|---|---|---|
| 阅读页控制层参照模板 - 基于修正版含亮度条 (P1 Fixed) - 箭头修复版 | f0413fa7d1024d1b8766aba2939ff266 | 否 |

## 3. 修改范围

| screen 名称 | Screen ID | 本轮处理方式 |
|---|---|---|
| 阅读页 - 目录底栏功能展开 (最终修正版) | de78704dcc3d44f2a1dbda8badbfb1f7 | 已直接编辑，部分公共层和目录内容有改动；仍需复修 |
| 阅读页 - 朗读底栏功能展开 (最终修正版) | b4668777331140a8b6006020183bfc00 | 已直接编辑，朗读区局部改善；仍残留旧控制层和英文标注 |
| 阅读页 - 界面底栏功能展开 (最终修正版) | fbf8ec6a3d0e4e02843b2cd0db66d71d | 已直接编辑，界面区局部变化；仍残留 45% 进度和错误底栏图标 |
| 阅读页 - 设置底栏功能展开 (最终修正版) | 2b6ea988559e4eae8c20166918c783d5 | 已直接编辑，设置项局部补齐；仍残留 h-[60vh] 嵌套、45% 进度和错误底栏图标 |

## 4. 母版公共层复制结果

| screen 名称 | 顶部第一行 | 顶部第二行 | 正文区域 | Quick Action Row | Chapter Jump Row | Bottom Function Row | 边界标注 | 是否通过 |
|---|---|---|---|---|---|---|---|---|
| 阅读页 - 目录底栏功能展开 (最终修正版) | 基本保留 | 基本保留 | 已恢复科幻正文 | 不通过：规格仍与其他 screen 不完全一致 | 基本为 25% | 不通过：仍检测到 format_size | 不通过：标签和边界风格仍偏重 | 否 |
| 阅读页 - 朗读底栏功能展开 (最终修正版) | 基本保留 | 已为描边 chip | 基本保留 | 不通过：仍有 flex justify-between、opacity-60、小字号按钮 | 25% 存在 | 不通过：仍有 format_size 和当前项强调 | 不通过：仍有 Playback Overlay / Control Buttons / Bottom Panel Group | 否 |
| 阅读页 - 界面底栏功能展开 (最终修正版) | 基本保留 | 已为描边 chip | 基本保留 | 基本文案正确 | 不通过：仍为 45% | 不通过：仍有 volume_up / text_format / 当前项强调 | 不通过：边界与其他 screen 不统一 | 否 |
| 阅读页 - 设置底栏功能展开 (最终修正版) | 基本保留 | 已为描边 chip | 基本保留 | 基本文案正确 | 不通过：仍为 45% | 不通过：仍有 volume_up / text_format | 不通过：仍有 h-[60vh] 面板嵌套和边界体系差异 | 否 |

## 5. 朗读展开区重构结果

| 分组 | 修复前问题 | 修复后表现 | 是否通过 | 备注 |
|---|---|---|---|---|
| 标题与状态 | 标题、状态、章节、时间压缩在同一块 | Stitch 生成了更明确的朗读标题、状态和章节表达 | 部分通过 | 仍残留英文 handoff label，结构未完全回到统一展开容器 |
| 主播放控制 | 使用 skip_previous，容易与章节跳转混淆 | 精修目标要求只保留播放/暂停和停止，但下载 HTML 仍检测到播放区域附近的 skip_previous | 否 | 语义冲突未确认消除 |
| 朗读进度 | 朗读时间和控制混在一起 | 有进度表达，但仍夹杂旧分组和旧标签 | 部分通过 | 需要重新明确为独立朗读进度组 |
| 朗读参数 | 语速、音量、定时、音色拥挤；定时和音色 affordance 不一致 | 有语速、音量、定时关闭、朗读音色内容 | 部分通过 | 仍需确认定时关闭和朗读音色已经上下列表化且右侧 chevron 一致 |

## 6. P0 修复结果表

| screen 名称 | 原 P0 | 修复方式 | 是否修复 | 备注 |
|---|---|---|---|---|
| 4 个 screen | Quick Action Row 视觉规格不一致 | 要求全部按母版统一为 grid-cols-4 四等分 | 否 | 朗读仍检测到 flex justify-between、opacity-60、小字号按钮；四屏 item 规格仍不一致 |
| 4 个 screen | Bottom Function Row 图标、入口尺寸和当前项强调不一致 | 要求统一 menu_book / record_voice_over / tune / settings，全部普通态 | 否 | 目录仍有 format_size；界面/设置仍有 volume_up / text_format；界面仍有当前项强调 |
| 4 个 screen | Bottom Control Stack 与展开区域容器关系不一致 | 要求展开区域在上、Bottom Control Stack 独立在下 | 否 | 设置仍检测到 h-[60vh] 面板嵌套；朗读仍有 Bottom Panel Group |
| 阅读页 - 朗读底栏功能展开 | 朗读信息架构不合理 | 要求重做为 4 个分组 | 部分修复 | 有改善但旧 label、旧控制层和可能的 skip_previous 残留仍在 |
| 阅读页 - 朗读底栏功能展开 | 播放控制与章节跳转语义混淆 | 要求播放区不使用 skip_previous / skip_next | 否 | 自检仍检测到 skip_previous 出现在朗读 HTML 的旧控制区域 |
| 阅读页 - 朗读底栏功能展开 | 定时关闭 / 朗读音色并排过窄且交互暗示不一致 | 要求改为上下列表项，右侧统一 chevron | 部分修复 | 内容存在，但需要继续按统一列表项彻底替换旧卡片布局 |

## 7. P1 修复结果表

| screen 名称 | 原 P1 | 修复方式 | 是否修复 | 备注 |
|---|---|---|---|---|
| 阅读页 - 目录底栏功能展开 | 缺少右侧快速滑动条或索引条 | 增加右侧细索引条和拖拽块 | 基本修复 | 自检看到目录区域已有右侧滑动条结构 |
| 阅读页 - 设置底栏功能展开 | 缺少自动缓存本章或后续章节 | 补充自动缓存本章 / 自动缓存后续章节 | 部分修复 | 第二次轮询 HTML 仍显示旧设置块，需继续确认最终 DOM 是否保留新增项 |
| 阅读页 - 设置底栏功能展开 | 缺少阅读页手势设置 | 补充阅读页手势设置分组 | 部分修复 | 第二次轮询 HTML 仍显示旧设置块，需继续确认最终 DOM 是否保留新增项 |
| 4 个 screen | 章节进度不一致 | 要求统一 25% | 否 | 界面 / 设置仍检测到 45% |
| 4 个 screen | 边界标注过重且不统一 | 要求轻量虚线并删除英文标签 | 否 | 朗读仍有 Playback Overlay / Control Buttons / Bottom Panel Group |

## 8. 是否仍有 P0

1. 朗读 screen 仍残留旧 Bottom Panel Group / Playback Overlay / Control Buttons 英文标注。
2. 朗读 screen 的 Quick Action Row 仍不是与其他 screen 完全一致的母版式 grid-cols-4。
3. 目录 / 朗读 / 界面 / 设置的 Bottom Function Row 图标仍未统一为 menu_book / record_voice_over / tune / settings。
4. 界面 / 设置 screen 的 Chapter Jump Row 仍检测到 45%，未统一为 25%。
5. 设置 screen 仍检测到 h-[60vh] 设置面板嵌套，Bottom Control Stack 容器关系未完全修复。
6. 朗读展开区仍未确认彻底移除播放区 skip_previous / skip_next 语义冲突。

## 9. 是否仍有 P1

1. 目录右侧索引条已尝试补齐，但仍需视觉复审确认不会遮挡章节文字。
2. 设置自动缓存与阅读页手势设置已尝试补齐，但下载 HTML 未稳定体现最终修复结果。
3. 边界标注整体仍偏重且不统一。
4. 朗读参数区仍需进一步确认定时关闭与朗读音色是否完全改成上下列表项并使用一致 chevron。

## 10. 是否建议进入只读复审

不建议。

原因：本轮已经直接编辑了 4 个现有最终修正版 screen，且母版未被修改；但 Stitch 编辑未能稳定、完整地把母版公共层复制到 4 个派生态。当前仍存在结构性 P0，尤其是底部三层控制层、底栏图标体系、章节进度、设置容器关系和朗读展开区旧结构残留。需要继续修复后再进入只读复审。
