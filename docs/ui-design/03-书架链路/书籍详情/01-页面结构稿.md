# 书籍详情页面结构稿

## 页面目标

展示书籍信息、目录预览、简介和书架状态，并承接所有书籍配置修改。

## 页面区域

1. 顶部标题或控制区。
2. 主内容区，承载当前任务的核心列表、表单或阅读覆盖层。
3. 操作反馈区，展示 loading、empty、error、offline 和 permission。
4. 返回区，必须保留来源页上下文。

## 自适应

- 以 `390x884dp` 为逻辑基准。
- 小屏优先保证主操作和返回入口可达。
- 图片仅作为视觉示例，实际布局按 token 和文字稿约束实现。
