# 书籍搜索资产与验收清单

状态：`COMPONENT_REFERENCE_NEEDS_VISUAL_VERIFICATION`
唯一全局约束：`../../01-全局规范/00-唯一约束参考.md`
图标资产注册表：`../../01-全局规范/图标资产/00-图标资产注册表.md`

## 1. 当前图片资产

| 资产 | 状态 | 用途 |
| --- | --- | --- |
| `图片/01-书籍搜索-首页.png` | `STATE_OR_VARIANT_REFERENCE` | 搜索首页状态输入；最终采用以截图叠放验收为准 |
| `图片/02-书籍搜索-结果页.png` | `PAGE_VISUAL_BASELINE` | 当前搜索结果默认母版；最终采用以截图叠放验收为准 |
| `图片/03-搜索状态-loading.png` | `STATE_OR_VARIANT_REFERENCE` | 当前页面视觉/状态输入；最终采用以截图叠放验收为准 |
| `图片/04-搜索状态-empty.png` | `STATE_OR_VARIANT_REFERENCE` | 当前页面视觉/状态输入；最终采用以截图叠放验收为准 |
| `图片/05-搜索状态-error.png` | `STATE_OR_VARIANT_REFERENCE` | 当前页面视觉/状态输入；最终采用以截图叠放验收为准 |

## 2. 页面级组件局部

| 组件局部 | 状态 | 要求 |
| --- | --- | --- |
| 顶部栏完整局部 | `COMPONENT_REFERENCE` | 已裁页面级完整上下文；仍需截图叠放验收 |
| 搜索框完整局部 | `COMPONENT_REFERENCE` | 已裁页面级完整上下文；仍需截图叠放验收 |
| 范围筛选完整局部 | `COMPONENT_REFERENCE` | 已裁页面级完整上下文；仍需截图叠放验收 |
| 结果摘要筛选完整局部 | `COMPONENT_REFERENCE` | 已裁页面级完整上下文；仍需截图叠放验收 |
| 结果卡片/列表完整局部 | `COMPONENT_REFERENCE` | 已裁页面级完整上下文；仍需截图叠放验收 |
| 首页/loading/empty/error 状态整页参考 | `COMPONENT_REFERENCE` | 已裁页面级完整上下文；仍需截图叠放验收 |

页面级 `组件局部/` 已建立；但未完成 HTML/Stitch 截图叠放验收前，不得标记为 `VISUAL_VERIFIED` 或 `STITCH_READY`。

## 3. 图标与可复用资产

| 资产类型 | 状态 | 约束 |
| --- | --- | --- |
| 全局 App Shell 图标 | `REFERENCE_BASELINE` | 以全局图标资产注册表为准；当前 PNG 抠图只作形状参考 |
| 阅读控制层图标 | `REFERENCE_BASELINE` | 以完整组件局部优先，SVG 重绘未复核前不得作为最终资产 |
| 页面专属插图/封面/背景 | `REFERENCE_BASELINE` | 以当前 UI 图为视觉来源；缺图页面必须补图后才能冻结 |

## 4. 验收门禁

进入 `VISUAL_VERIFIED` 前必须完成：

1. 裁出本页所有必需完整组件局部。
2. 标注每个组件局部的来源图片、裁剪范围和用途。
3. HTML/Stitch 输出与当前 UI 图同尺寸截图对比。
4. 主模块位置误差目标 `±2px`；圆角、边框、字号误差目标 `±1px`。
5. 对不满足误差的区域补充人工复核结论。

当前结论：`COMPONENT_REFERENCE_NEEDS_VISUAL_VERIFICATION`。本文件只登记资产和验收状态，不代表页面已经通过视觉复刻验收。
