# 本地书导入文案稿

状态：`FORMAL_DRAFT_READY`

## 1. 默认页文案

| 位置 | 文案 |
| --- | --- |
| 页面标题 | `导入本地书` |
| 主标题 | `选择本地书籍` |
| 说明 | `通过系统文件选择器导入文件，不会扫描整个存储。` |
| 支持格式 | `支持 TXT、EPUB 等本地书籍文件` |
| 主按钮 | `选择文件` |

## 2. 解析中文案

| 位置 | 文案 |
| --- | --- |
| 标题 | `正在导入` |
| 说明 | `正在解析文件，请稍候` |
| 当前文件 | `正在解析：{fileName}` |

## 3. 结果文案

| 场景 | 标题 | 说明 | 主按钮 | 次按钮 |
| --- | --- | --- | --- | --- |
| 成功 | `导入完成` | `已将选中的书籍加入书架` | `完成` | - |
| 部分失败 | `部分文件导入失败` | `可以查看失败原因后重新选择` | `重新选择` | `完成` |
| 全部失败 | `导入失败` | `未能导入选中的文件` | `重新选择` | `返回书架` |

## 4. 文件结果文案

- `已导入`
- `已存在，已跳过`
- `格式不支持`
- `解析失败`

## 5. 文案规则

- 不使用技术字段名。
- 按钮文案必须对应真实结果，不用含糊的 `确定` 替代。
- 错误文案必须说明原因和下一步。
