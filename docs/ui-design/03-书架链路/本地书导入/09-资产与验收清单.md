# 本地书导入资产与验收清单

状态：`PAGE_VISUAL_CANDIDATE`
唯一全局约束：`../../01-全局规范/00-唯一约束参考.md`

## 1. 当前图片资产

| 资产 | 状态 | 用途 |
| --- | --- | --- |
| `图片/01-本地书导入-视觉demo候选.png` | `PAGE_VISUAL_CANDIDATE` | image2 生成的结果态视觉候选，已裁组件局部，尚未作为本页正式视觉基准冻结 |

## 2. 页面级组件局部

已基于候选图裁出页面级完整组件局部。候选图确认前，这些组件局部只能作为 `COMPONENT_REFERENCE_CANDIDATE`。

## 3. 补齐后必须裁剪

| 组件局部 | 要求 |
| --- | --- |
| 顶部栏完整局部 | `COMPONENT_REFERENCE_CANDIDATE` |
| 导入说明卡完整局部 | `COMPONENT_REFERENCE_CANDIDATE` |
| 导入结果摘要完整局部 | `COMPONENT_REFERENCE_CANDIDATE` |
| 文件列表完整局部 | `COMPONENT_REFERENCE_CANDIDATE` |
| 底部操作栏完整局部 | `COMPONENT_REFERENCE_CANDIDATE` |

## 4. 验收门禁

进入 `VISUAL_VERIFIED` 前必须完成：

1. 确认候选图是否作为本页当前 UI 图。
2. 如确认采用，将资产状态升级为 `PAGE_VISUAL_BASELINE`。
3. HTML/Stitch 输出与当前 UI 图同尺寸截图叠放。
4. 主模块位置误差目标 `±2px`；圆角、边框、字号误差目标 `±1px`。

当前结论：`PAGE_VISUAL_CANDIDATE`。已有候选图和组件局部，但尚未确认其为正式页面视觉基准；不得标记为 `VISUAL_VERIFIED` 或 `STITCH_READY`。
