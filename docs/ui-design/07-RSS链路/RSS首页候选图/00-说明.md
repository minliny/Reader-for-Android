# RSS 首页候选图

从旧 `07-RSS链路/RSS首页候选图/图片/01-RSS首页订阅流.png` 迁入。当前只作为 RSS 主标签页根页面的来源历史和后续链路参考。

状态：`REFERENCE_ONLY`，退出当前实现。

## 当前实现边界

- 不作为 RSS 主 Tab 当前 source。
- 不进入 P0 Stitch 当前输入。
- 不用于单独冻结 RSS 链路页面。
- RSS 主 Tab 当前实现以 `docs/ui-design/02-主标签页/RSS/` 页面包为准。
