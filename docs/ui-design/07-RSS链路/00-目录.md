# RSS 链路目录

当前 RSS 完整链路暂缓。本目录只保留 RSS 首页候选图作为 `REFERENCE_ONLY` 参考归档，退出当前实现，不进入 P0 Stitch 当前输入。

当前 RSS 主 Tab 以 `docs/ui-design/02-主标签页/RSS/` 页面包为准。后续恢复 RSS 链路时，需要重新补订阅管理、添加订阅源和条目详情页面包，不能直接复用本目录候选图作为当前 source。
