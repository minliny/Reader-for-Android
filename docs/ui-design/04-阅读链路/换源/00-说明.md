# 换源

状态：P1 页面包，不进入 P0 冻结

## 页面定位

在阅读中检查和切换可用来源，处理来源失效与检测结果。

## 优先级

P1。换源不进入 P0 当前实现冻结，不阻塞 P0 Stitch 输入；P0 只保留阅读控制层中的异常修复入口语义，不要求完整换源页面复原。

## 入口

阅读控制层顶部更多菜单、书籍详情来源入口。

## 图片示例

- `图片/01-阅读页换源流程.png`
- `图片/02-来源列表.png`
- `图片/03-来源检查中.png`
- `图片/04-来源检查结果.png`

## 权威约束

本目录内标准 `01-06` 文字稿为 Stitch 复原和实现约束入口；已有专项稿继续作为细化资料。

## 当前实现边界

- 不作为 P0 必交页面。
- 不进入 P0 Stitch 当前输入包。
- 可作为 P1 阅读异常修复链路，在 P0 页面中仅保留入口和跳转关系。

## 约束继承

- 唯一全局约束：`../../01-全局规范/00-唯一约束参考.md`
- 文档权威索引：`../../01-全局规范/07-文档权威索引.md`
- 当前 UI 设计图优先定义视觉比例、密度、颜色、圆角、阴影、边距和字号。
- 本目录文字稿定义页面结构、入口、交互、状态、文案和禁止项。
- 未具备完整组件局部和截图叠放验收前，不得标记为 `STITCH_READY` 或 `IMPLEMENTATION_READY`。
