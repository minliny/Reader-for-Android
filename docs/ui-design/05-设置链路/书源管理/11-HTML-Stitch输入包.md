# 书源管理 HTML/Stitch 输入包

状态：`STITCH_INPUT_READY_NEEDS_GENERATION`
页面等级：`P1 / SETTINGS`
当前输出门禁：`HTML_DRAFT`

本文件只定义生成输入，不代表 HTML 已通过视觉复刻验收。输出必须完成同尺寸截图叠放后，才能升级为 `VISUAL_VERIFIED` 或 `STITCH_READY`。

## 1. 基准输出

| 项目 | 约束 |
| --- | --- |
| 当前页面视觉基准 | `图片/01-书源管理首页.png` |
| 基准尺寸 | `836 x 1881` |
| 生成目标 | 生成书源管理首页；详情、新增、编辑、检测、调试、错误日志为状态参考 |
| 截图验收 | HTML 截图必须按基准尺寸与页面视觉基准截图叠放 |

## 2. 输入读取顺序

1. `../../01-全局规范/00-唯一约束参考.md`
2. `10-正式UI设计稿.md`
3. 页面视觉规格 `08-*视觉规格.md`
4. `09-资产与验收清单.md`
5. `组件局部/00-组件局部索引.md`
6. `01-页面结构稿.md` 到 `06-禁止项.md`
7. `图片/01-书源管理首页.png`
8. `图片/*.png` 状态参考
9. `组件局部/*.png`

视觉比例、模块大小、边框、圆角、颜色、阴影、文字密度优先取当前 UI 设计图和完整组件局部；文字稿负责页面语义、状态和交互结果。

## 3. 图片角色

| 图片 | 角色 | 使用方式 |
| --- | --- | --- |
| `图片/01-书源管理首页.png` | `PAGE_VISUAL_BASELINE` | 当前整页页面视觉基准 |
| `图片/02-书源详情.png` | `STATE_OR_VARIANT_REFERENCE` | 状态/变体参考 |
| `图片/03-新增书源.png` | `STATE_OR_VARIANT_REFERENCE` | 状态/变体参考 |
| `图片/04-编辑书源.png` | `STATE_OR_VARIANT_REFERENCE` | 状态/变体参考 |
| `图片/05-书源检测中.png` | `STATE_OR_VARIANT_REFERENCE` | 状态/变体参考 |
| `图片/06-书源检测结果.png` | `STATE_OR_VARIANT_REFERENCE` | 状态/变体参考 |
| `图片/07-书源调试信息.png` | `STATE_OR_VARIANT_REFERENCE` | 状态/变体参考 |
| `图片/08-书源错误日志.png` | `STATE_OR_VARIANT_REFERENCE` | 状态/变体参考 |

组件局部当前共 `12` 个 PNG 参考，详见 `组件局部/00-组件局部索引.md`。

## 4. 页面生成约束

书源管理首页为母版，多状态图不得混成一个页面；检测和调试为派生状态。

生成时必须保持当前页面视觉基准的结构层级和组件关系。多状态图片只用于派生状态，不得把多个状态合并成一个页面。历史、旧版、废弃、候选图片不能反向覆盖当前页面视觉基准。

响应式以当前页面视觉基准尺寸为基准。小屏优先保留顶部结构、核心内容、主要操作和安全区；短屏允许内容区滚动，不通过缩小字号破坏可读性。

## 5. 验收清单

| 项目 | 通过标准 |
| --- | --- |
| 输出状态 | 初次生成只能标记 `HTML_DRAFT` |
| 截图尺寸 | 与当前页面视觉基准尺寸一致 |
| 主模块位置 | 顶部、核心内容、主要操作目标误差 `±2px` |
| 视觉 token | 圆角、边框、字号、行高目标误差 `±1px` |
| 状态一致性 | 状态参考只替换对应状态区域，不重画页面外壳 |
| 人工复核 | 截图叠放后记录差异，未复核前不得升级为 `STITCH_READY` |
