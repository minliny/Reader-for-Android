# 书源管理页面结构稿

## 页面目标

管理书源列表、详情、新增编辑、检测和错误日志。

## 入口

设置首页书源管理入口、发现页来源管理入口。

## 页面区域

1. 顶部安全区和标题区：提供页面标题、返回或关闭入口。
2. 主内容区：承载本页面核心内容，滚动时不得遮挡底部操作或系统手势区。
3. 操作区：主操作优先，危险操作必须二次确认。
4. 反馈区：loading、empty、error、offline 使用统一状态组件。

## 图片示例

- `图片/01-书源管理首页.png`
- `图片/02-书源详情.png`
- `图片/03-新增书源.png`
- `图片/04-编辑书源.png`
- `图片/05-书源检测中.png`
- `图片/06-书源检测结果.png`
- `图片/07-书源调试信息.png`
- `图片/08-书源错误日志.png`

## 自适应规则

- 手机竖屏以 `390x884dp` 为逻辑基准。
- 宽度小于 `360dp` 时使用紧凑边距，长文案换行，不压缩按钮触控区。
- 高度不足时优先让主内容区滚动，顶部标题和关键操作保持可达。
- 页面不得依赖固定像素截图；组件尺寸以全局 token 和页面包文字稿为准。

## 专项约束补充

### 页面树

```text
TopBar(title=书源管理)
ContentScroll
SettingGroupCard
SettingRow
StateLayer
```

### 结构执行规则

- 当前页面结构以本页面树和 `10-正式UI设计稿.md` 为准。
- `图片/` 目录中的当前 UI 图只定义视觉比例和密度，不允许反向新增页面树外模块。
- 顶部、滚动区、底表/浮层和状态层必须分层实现，不能合并成单个自由排版容器。
