# 书源管理资产与验收清单

状态：`VISUAL_REVIEW_REQUIRED`
唯一全局约束：`../../01-全局规范/00-唯一约束参考.md`
图标资产注册表：`../../01-全局规范/图标资产/00-图标资产注册表.md`

## 1. 当前图片资产

| 资产 | 状态 | 用途 |
| --- | --- | --- |
| `图片/01-书源管理首页.png` | `PAGE_VISUAL_BASELINE` | 当前页面视觉/状态输入；最终采用以截图叠放验收为准 |
| `图片/02-书源详情.png` | `STATE_OR_VARIANT_REFERENCE` | 当前页面视觉/状态输入；最终采用以截图叠放验收为准 |
| `图片/03-新增书源.png` | `STATE_OR_VARIANT_REFERENCE` | 当前页面视觉/状态输入；最终采用以截图叠放验收为准 |
| `图片/04-编辑书源.png` | `STATE_OR_VARIANT_REFERENCE` | 当前页面视觉/状态输入；最终采用以截图叠放验收为准 |
| `图片/05-书源检测中.png` | `REFERENCE_BASELINE` | 当前页面视觉/状态输入；最终采用以截图叠放验收为准 |
| `图片/06-书源检测结果.png` | `STATE_OR_VARIANT_REFERENCE` | 当前页面视觉/状态输入；最终采用以截图叠放验收为准 |
| `图片/07-书源调试信息.png` | `REFERENCE_BASELINE` | 当前页面视觉/状态输入；最终采用以截图叠放验收为准 |
| `图片/08-书源错误日志.png` | `REFERENCE_BASELINE` | 当前页面视觉/状态输入；最终采用以截图叠放验收为准 |

## 2. 页面级组件局部

| 组件局部 | 状态 | 要求 |
| --- | --- | --- |
| TopBar 或标题区完整局部 | `NEEDS_COMPONENT_CROP` | 从当前 UI 设计图裁完整上下文，不使用单独小图标替代 |
| 主内容模块完整局部 | `NEEDS_COMPONENT_CROP` | 从当前 UI 设计图裁完整上下文，不使用单独小图标替代 |
| 列表/卡片/表单完整局部 | `NEEDS_COMPONENT_CROP` | 从当前 UI 设计图裁完整上下文，不使用单独小图标替代 |
| 状态层完整局部 | `NEEDS_COMPONENT_CROP` | 从当前 UI 设计图裁完整上下文，不使用单独小图标替代 |
| 底部操作或浮层完整局部 | `NEEDS_COMPONENT_CROP` | 从当前 UI 设计图裁完整上下文，不使用单独小图标替代 |

页面级 `组件局部/` 缺失时，不得把页面标记为 `COMPONENT_REFERENCE`、`STITCH_READY` 或 `VISUAL_VERIFIED`。

## 3. 图标与可复用资产

| 资产类型 | 状态 | 约束 |
| --- | --- | --- |
| 全局 App Shell 图标 | `REFERENCE_BASELINE` | 以全局图标资产注册表为准；当前 PNG 抠图只作形状参考 |
| 阅读控制层图标 | `REFERENCE_BASELINE` | 以完整组件局部优先，SVG 重绘未复核前不得作为最终资产 |
| 页面专属插图/封面/背景 | `REFERENCE_BASELINE` | 以当前 UI 图为视觉来源；缺图页面必须补图后才能冻结 |

## 4. 验收门禁

进入 `VISUAL_VERIFIED` 前必须完成：

1. 裁出本页所有必需完整组件局部。
2. 标注每个组件局部的来源图片、裁剪范围和用途。
3. HTML/Stitch 输出与当前 UI 图同尺寸截图对比。
4. 主模块位置误差目标 `±2px`；圆角、边框、字号误差目标 `±1px`。
5. 对不满足误差的区域补充人工复核结论。

当前结论：`VISUAL_REVIEW_REQUIRED`。本文件只登记资产和验收状态，不代表页面已经通过视觉复刻验收。

## 5. 本轮组件局部补齐

页面级 `组件局部/` 已建立，当前共 `12` 个 PNG 参考（不含索引 Markdown）。未完成 HTML/Stitch 截图叠放验收前，不得标记为 `VISUAL_VERIFIED` 或 `STITCH_READY`。

当前结论：`COMPONENT_REFERENCE_NEEDS_VISUAL_VERIFICATION`。
