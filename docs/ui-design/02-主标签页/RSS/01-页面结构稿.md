# RSS 首页 Page Spec

## 页面定位

RSS 首页是底部主导航 `RSS` 对应的根页面，用于查看订阅源最新内容流。当前阶段只保留首页结构。

## 页面结构

1. 系统状态栏安全区。
2. 顶部栏：标题 `RSS`，右侧搜索、更多。
3. `订阅概览` 卡片：订阅源数量、未读数量、更新时间、刷新入口。
4. 内容筛选 chip：`全部 / 未读 / 收藏 / 稍后读 / 书单`。
5. 订阅源筛选 chip：`全部来源 / 小说更新 / 技术文章 / 书单推送`。
6. `最新订阅` 列表。
7. 底部主导航：当前高亮 `RSS`。

## 范围边界

- 当前只生成 RSS 首页。
- RSS 搜索、订阅管理、添加订阅源、条目详情、分组管理暂缓。
- 行内三点菜单只保留入口，不展开菜单设计。
