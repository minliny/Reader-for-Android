# RSS 首页 State Spec

| 状态 | 当前要求 |
| --- | --- |
| 默认态 | 使用 `01-RSS首页-订阅流.png` |
| 刷新中 | 保留订阅概览和筛选，列表局部 loading |
| 无订阅 | 使用 AppShell 空态，主操作 `添加订阅源` 后续定义 |
| 无未读 | 保留筛选上下文，提示当前筛选无内容 |
| 网络失败 | 保留上次内容或框架，提供 `重试` |

当前阶段不补 RSS loading/empty/error 图片；后续补图时必须复用 `AppShell.State.*`。
