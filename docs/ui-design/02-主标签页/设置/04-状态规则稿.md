# 设置首页 State Spec

| 状态 | 当前要求 |
| --- | --- |
| 默认态 | 使用 `01-设置首页-管理入口.png` |
| 局部数据加载 | 概览卡显示骨架或占位，入口列表保持可见 |
| 无备份 | `最近备份` 显示 `未设置` |
| 权限缺失 | 对应行显示状态提示，不弹全屏错误 |
| 搜索/更多 | 当前只保留入口，不补弹层状态 |

设置首页不需要全屏空态；如果本地统计为空，也应显示可进入的管理入口。
