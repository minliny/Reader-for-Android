# 书架链路剩余输入包补齐审计

日期：2026-06-14
状态：`BOOKSHELF_FLOW_REMAINING_INPUT_PACKAGE_COMPLETED`

## 1. 审计结论

本轮补齐 `分组管理`、`排序与筛选` 两个书架链路页面的完整组件局部、资产验收状态和 `11-HTML-Stitch输入包.md`。至此书架链路中除 `本地书导入` 外，已有 UI 图的页面均具备 HTML/Stitch 输入包。生成后的 HTML 初稿仍只能标记为 `HTML_DRAFT`，必须截图叠放通过后才能升级。

## 2. 本轮补齐页面

| 页面 | 当前母版 | 组件局部数 | 输入包 | 状态 |
| --- | --- | ---: | --- | --- |
| 分组管理 | `../03-书架链路/分组管理/图片/01-书架分组管理.png` | 6 | `../03-书架链路/分组管理/11-HTML-Stitch输入包.md` | `STITCH_INPUT_READY_NEEDS_GENERATION` |
| 排序与筛选 | `../03-书架链路/排序与筛选/图片/01-书架排序与筛选.png` | 8 | `../03-书架链路/排序与筛选/11-HTML-Stitch输入包.md` | `STITCH_INPUT_READY_NEEDS_GENERATION` |

## 3. 组件局部范围

| 页面 | 已裁组件 |
| --- | --- |
| 分组管理 | 顶部栏、系统分组行、自定义分组行、分组列表、新建分组弹窗、整页参考 |
| 排序与筛选 | 背景书架、底表整体、底表标题、排序方式、排序顺序、筛选条件、底部操作栏、整页参考 |

## 4. 特殊约束

`排序与筛选` 是书架页面上的底表浮层，不是独立全屏页面。背景保留书架页面并叠加遮罩，点击 `应用` 前不刷新列表。

`分组管理` 的新建分组弹窗是页面状态，不是独立页面；删除分组不删除书籍，系统分组不可编辑。

## 5. 剩余阻塞

`本地书导入` 当前没有独立 UI 图，不能进入 `STITCH_INPUT_READY_NEEDS_GENERATION`。需要先补当前视觉母版，再裁完整组件局部并建立输入包。
