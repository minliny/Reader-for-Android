# 主 Tab 输入包补齐审计

日期：2026-06-14
状态：`MAIN_TAB_INPUT_PACKAGE_COMPLETED`

## 1. 审计结论

本轮补齐 RSS、发现、设置三个主 Tab 根页面的完整组件局部、资产验收状态和 `11-HTML-Stitch输入包.md`。至此主 Tab 四个根页面均可进入 HTML 初稿生成，但仍统一停留在 `HTML_DRAFT` 门禁，必须完成同尺寸截图叠放后才能升级。

## 2. 本轮补齐页面

| 页面 | 当前母版 | 组件局部数 | 输入包 | 状态 |
| --- | --- | ---: | --- | --- |
| RSS | `../02-主标签页/RSS/图片/01-RSS首页-订阅流.png` | 7 | `../02-主标签页/RSS/11-HTML-Stitch输入包.md` | `STITCH_INPUT_READY_NEEDS_GENERATION` |
| 发现 | `../02-主标签页/发现/图片/01-发现首页-信息源分类.png` | 9 | `../02-主标签页/发现/11-HTML-Stitch输入包.md` | `STITCH_INPUT_READY_NEEDS_GENERATION` |
| 设置 | `../02-主标签页/设置/图片/01-设置首页-管理入口.png` | 6 | `../02-主标签页/设置/11-HTML-Stitch输入包.md` | `STITCH_INPUT_READY_NEEDS_GENERATION` |

## 3. 组件局部范围

| 页面 | 已裁组件 |
| --- | --- |
| RSS | 顶部栏、订阅概览卡片、状态分组 chip、订阅源分类、最新订阅列表、底部导航、整页参考 |
| 发现 | 顶部栏、搜索框、信息源卡片、分类 chip、推荐书籍、来源状态条、来源更新列表、底部导航、整页参考 |
| 设置 | 顶部栏、本地概览卡片、常用管理网格、全部设置列表、底部导航、整页参考 |

## 4. 统一约束

三个页面均为 `P0 / MAIN_TAB_ROOT`，必须继承书架 App Shell token：页面背景、顶部栏、底部四项导航、导航高度、图标尺寸、文字字号、卡片圆角、边框、阴影和激活态颜色。

HTML 生成时优先读取当前 UI 设计图和完整组件局部；文字稿只负责页面语义、入口、状态和交互结果。

## 5. 下一步

1. 按输入包生成主 Tab 四页 HTML 初稿。
2. 对每个 HTML 以母版尺寸截图。
3. 与母版进行截图叠放，记录 `±2px` 模块误差和 `±1px` token 误差。
4. 通过后再考虑从 `HTML_DRAFT` 升级到 `VISUAL_VERIFIED`。
