# 设计稿重新审查记录

本文档基于“新增图片不覆盖旧图，使用 `16-1` 这类派生编号”的规则，重新审查当前 `docs/ui-design/91-历史归档/drafts/旧全局草稿目录` 中的示意图和对应规格。

## 结论

当前主图 `01` 到 `40` 中，`16-toc-overlay.png` 与 `17-toc-full.png` 已恢复为最开始认可的目录设计稿，不需要再改图。后续补图必须作为派生图新增，例如 `16-1`、`17-1`，不得覆盖 `16`、`17`。

本轮已补充目录/书签相关派生状态图，而不是重新生成目录主图。

## 命名与保存审查

| 项目 | 当前状态 | 处理意见 |
| --- | --- | --- |
| 主图覆盖风险 | 已发生过误覆盖风险 | 新图使用派生编号，不覆盖主图 |
| `16-toc-overlay.png` | 已恢复为历史认可版本 | 保留不动 |
| `17-toc-full.png` | 已恢复为历史认可版本 | 保留不动 |
| `_rejected/41-reader-directory-full-page-flow.png` | 后续误生成，视觉和流程均不作为依据 | 已移入 `_rejected/`，不参与正式索引 |
| `_contact_sheets/` | 排查缓存用临时图 | 不进入正式设计稿索引 |

## 目录/书签设计审查

| 审查项 | 最开始设计稿要求 | 当前文档状态 | 是否需要补充 |
| --- | --- | --- | --- |
| 阅读中目录覆盖层 | 由阅读控制层底栏 `目录` 进入 | 已在 `toc.md` 与 `page-flows.md` 校正 | 不需要改主图 |
| 完整目录页 | 从书籍详情的目录预览或全部目录进入 | 已校正，不再从覆盖层进入 | 不需要改主图 |
| 目录层级 | 示例图使用 `部 / 卷 / 章` 复杂样本 | 已补充扁平章节、卷+章、自定义分组的自适应规则 | 不需要改主图 |
| 章节行 | 无左竖线、无圆点、无行级箭头、无行级三点 | 已写入 `toc.md` | 不需要改主图 |
| 右侧状态 | 仅缓存与书签固定槽位 | 已写入 `toc.md` | 不需要改主图 |
| 书签标签 | 覆盖层和完整页都应可切换到书签 | 已补 `16-1`、`17-1` | 已完成 |
| 目录搜索 | 图上有搜索入口 | 已补 `16-2` | 已完成 |
| 三点菜单 | 图上有菜单入口 | 已补 `16-3` | 已完成 |

## 建议新增图片

| 建议文件 | 基于主图 | 内容 |
| --- | --- | --- |
| `16-1-toc-overlay-bookmark-tab.png` | `16-toc-overlay.png` | 阅读中目录覆盖层切换到 `书签` 标签后的状态 |
| `17-1-toc-full-bookmark-tab.png` | `17-toc-full.png` | 完整目录页切换到 `书签` 标签后的状态 |
| `16-2-toc-overlay-search-state.png` | `16-toc-overlay.png` | 覆盖层内搜索章节/书签的输入状态 |
| `16-3-toc-overlay-more-menu.png` | `16-toc-overlay.png` | 覆盖层三点菜单：刷新目录、缓存目录、折叠/展开层级 |

## 与当前规划的差异

当前规划已经修正了最大偏差：阅读中的目录覆盖层不再跳转到完整目录页。仍需注意以下差异：

- 不把目录页和换源、书源检测、来源状态混在一起。
- 不为目录章节行增加左侧进度线、圆点、未读状态或行级操作按钮。
- 不把派生状态图当成新的主视觉方向；派生图只解释某个入口或状态。
- 不用 `_rejected/41-reader-directory-full-page-flow.png` 推导后续页面。

## 本轮执行结果

1. 已补 `16-1` 覆盖层书签标签。
2. 已补 `17-1` 完整目录页书签标签。
3. 已补 `16-2` 搜索状态。
4. 已补 `16-3` 三点菜单状态。
5. 已同步更新 `91-历史归档/drafts/旧全局草稿目录/README.md` 和 `04-阅读链路/目录与书签/00-说明.md` 的相关示意图列表。
