# UI_FREEZE_READER_CONTROL_LAYER_V1 冻结前 P0 收敛整理

## 总体状态

`FREEZE_PREP_READY_FOR_DESIGN_DECISIONS`

本目录是冻结前整理目录，不是正式冻结包。当前资料可以被整理为候选清单、已完成位置对齐图和开放决策，但仍不能直接生成正式 `UI_FREEZE_READER_CONTROL_LAYER_V1`。

## 本轮范围

仅整理阅读控制层相关资料：

- 阅读沉浸页
- 阅读控制层默认态
- 顶部控制栏
- 控制层内部章节进度条
- 底部四主按钮工具栏
- 亮度辅助状态
- 章节进度辅助状态
- 目录/书签快捷模块与覆盖层
- 内容搜索入口/覆盖层
- 自动翻页快捷覆盖层
- 内容替换入口/覆盖层
- 朗读快捷区、运行态、设置页
- 界面快捷区及外观管理入口
- 设置快捷区
- 顶部更多菜单

## 不包含

- 书架、书籍搜索、书籍详情等全 App 页面冻结。
- Android、iOS、Web 前端代码。
- 新 UI 图生成或 Stitch 输出。
- git commit / push。

## 关键结论

- 已建立候选图清单，`03 / 04-2` 已确认不变；`04-2` 是唯一视觉与几何基线。
- 已明确 `_contact_sheets/`、`_progress_restore_backup/`、`_rejected/` 不得作为 final 来源。
- 已建立状态矩阵、组件清单、视觉 token、交互规则和验收标准草案。
- 已确认 `28-34` 不进入冻结候选，`35-39` 作为完整界面全页面候选。
- 已产出 `16-6 / 18-4 / 21-2 / 22-3` 的 `04-2` 标准版；旧图
  `16-5 / 18-3 / 21-1 / 22-2` 仅保留内容参考。
- `19-1 / 25-4` 保留为交互参考候选，仍需按 `04-2` 锚点复核。
- 正式冻结包仍需人工确认基准画布、底部规则、快捷界面与完整界面的纳入范围。

## 已确认决策

| 决策 | 结论 |
| --- | --- |
| 阅读沉浸页和默认控制层 | `03 / 04-2` 不变；`04-2` 是唯一视觉与几何基线。 |
| 外观拆解图 | `28-34` 不使用。 |
| 完整界面全页面 | `35-39` 作为完整界面全页面候选。 |
| 控制层快捷图调整方式 | `16-6 / 18-4 / 21-2 / 22-3` 为当前候选；旧图只作内容参考。 |
| 界面状态替代关系 | `34` 不使用；快捷界面以 `21-2` 为候选，完整界面全页面以 `35-39` 为候选。 |
