# 开放决策清单

## 必须人工确认

1. 基准画布是否统一为 `390 x 884`。
2. 当前 PNG 大尺寸是否视为 `390 x 884` 的等比例导出。
3. 状态栏和底部安全区是否包含在设计图内。
4. 已确认：`28-34` 不使用，`35-39` 作为完整界面全页面候选。
5. 朗读运行态和朗读设置页是否纳入 V1。
6. 顶部更多菜单是否纳入 V1。
7. 内容搜索/内容替换是否作为 V1 全页弹层纳入，还是只保留入口关系。
8. 自动翻页是否纳入 V1，还是只保留入口关系。
9. 底部阅读细进度条是否在沉浸态完全禁用。
10. 控制层快捷模块是否允许替换默认章节进度区，还是必须保留弱化章节进度信息。
11. 亮度状态和章节进度拖动状态是否需要标准手机 final 图。
12. 目录快捷模块与目录覆盖层是否都纳入 V1，还是只纳入快捷模块。
13. 完整管理页是否纳入阅读控制层 V1，尤其是朗读设置页、字体、主题、版式、翻页动画。
14. 顶部换源流程是否排除在 V1 之外，或作为顶部栏辅助状态纳入。

## 已关闭决策

| 决策 | 结论 |
| --- | --- |
| `03 / 04` 调整方式 | 不变，作为候选。 |
| 外观图组选择 | `28-34` 不用；`35-39` 作为完整界面全页面。 |
| `16-4 / 18-2 / 19 / 22-1 / 25-1` 调整方式 | 已完成高度对齐重画，当前采用 `16-5 / 18-3 / 19-1 / 22-2 / 25-4`。 |
| `34` 的角色 | 不使用。 |
