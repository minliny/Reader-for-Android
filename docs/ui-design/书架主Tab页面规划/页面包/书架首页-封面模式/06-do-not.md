# 书架首页封面模式 禁止项

- 不展示未加入书架的网络搜索结果。
- 不在书架首页条目中展示书源。
- 不放账户、头像、登录入口。
- 不把发现/RSS 内容混入书架首页。
- 不把候选图中的三列布局写成固定规则。
- 不把候选图中的分组示例写成固定分组。
- 不让封面模式和列表模式生成两套顶栏、chip、卡片、进度条或底部导航 token。
- 不为了适配小屏压缩文字到不可读；应减少列数或重排顶部信息区。
- 不在短屏上让底部导航遮挡书籍卡片操作区。
