# 页面流程

状态：`REFERENCE_ONLY`
当前权威：全局约束以 `../01-全局规范/00-唯一约束参考.md` 为准；页面交互以各页面包 `10-正式UI设计稿.md` 和 `03-交互规则稿.md` 为准。
说明：本文件为历史流程总览，不定义当前 UI 视觉、页面结构或验收标准。

本文档记录静态截图不容易表达的页面跳转关系。各页面的详细按钮行为仍以对应规格文档中的交互矩阵为准。

阅读控制层的最新动作、互斥、覆盖和返回规则以
`READER_CONTROL_ACTION_FLOWS.md` 为准；本文件只保留全 App 层级的流程总览。

## 阅读控制层

```mermaid
flowchart TD
  A1["阅读页 - 沉浸模式"] --> A2["阅读控制层"]
  A2 --> A2L["左侧主内容区"]
  A2 --> A2B["底部四主按钮"]
  A2 --> A2R["右侧亮度条"]

  A2L --> A2D["默认模块：快捷功能区 + 章节进度"]

  A2D -- "搜索" --> A3["内容搜索 - 阅读中覆盖层"]
  A3 -- "完整搜索 / 筛选" --> A4["内容搜索页"]
  A3 -- "结果行" --> A1
  A4 -- "结果行" --> A1

  A2D -- "自动翻页" --> A5["自动翻页设置覆盖层"]
  A5 -- "开始自动翻页" --> A6["自动翻页运行胶囊"]
  A5 -- "更多设置" --> A7["自动翻页完整设置页"]

  A2D -- "替换" --> A8["替换快捷覆盖层"]
  A8 -- "全部规则" --> A9["替换规则页"]
  A8 -- "新增规则 / 规则行" --> A10["替换规则编辑页"]
  A9 -- "新增规则 / 规则行" --> A10

  A2B -- "目录" --> A11["目录快捷模块"]
  A11 -- "章节行" --> A1
  A11 -- "完整目录 >" --> A12["完整目录页"]

  A2B -- "朗读" --> A13["朗读快捷模块"]
  A13 -- "开始朗读" --> A14["朗读运行胶囊"]
  A13 -- "音色 / 范围 / 定时" --> A15["朗读参数选择"]
  A13 -- "更多设置" --> A16["朗读设置页"]

  A2B -- "界面" --> A17["界面快捷模块"]
  A17 -- "字体 默认 >" --> A18["字体选择"]
  A17 -- "主题 更多 >" --> A19["主题选择"]
  A19 -- "自定义主题 >" --> A20["主题编辑"]
  A17 -- "更多版式 >" --> A21["版式高级"]
  A17 -- "翻页动画 覆盖 >" --> A22["翻页动画"]

  A2B -- "设置" --> A23["设置快捷模块"]
  A23 -- "完整设置 >" --> A24["完整阅读设置页"]
  A24 -- "屏幕与显示行为" --> A25["屏幕与显示行为"]
  A24 -- "翻页与手势" --> A26["翻页与手势"]
  A24 -- "进度与信息显示" --> A27["进度与信息显示"]
  A24 -- "阅读行为高级" --> A33["阅读行为高级"]
  A24 -- "右上菜单" --> A34["设置预设"]

  A2 -- "夜间模式" --> A28["夜间控制层"]
  A2 -- "顶部换源" --> A29["换源底表"]
  A2 -- "顶部三点菜单" --> A30["阅读维护菜单"]
  A2R -- "系统亮度" --> A31["跟随系统亮度"]
  A2D -- "章节进度滑条" --> A32["章节内定位"]
```

## 书架与书籍详情

`B6 本地书导入` 纳入当前功能范围，但不阻塞 Reader Control Layer freeze。V1 先定义
系统文件选择器入口、存储权限、导入进度、成功/部分失败/失败反馈；专用页面图列入
Bookshelf freeze 的 P1 输入，route 为 `bookshelf_import`（URL `/bookshelf/import`）。

```mermaid
flowchart TD
  B1["书架"] --> B2["书籍信息页"]
  B1 -- "切换视图" --> B3["书架列表模式"]
  B1 -- "分组管理" --> B4["书架分组管理"]
  B1 -- "更多" --> B5["书籍更多菜单"]
  B1 -- "导入本地书" --> B6["本地书导入"]
  B2 -- "开始阅读 / 继续阅读" --> B7["阅读页"]
  B2 -- "目录预览" --> B8["书籍详情目录预览"]
  B8 -- "章节行" --> B7
  B8 -- "全部目录" --> B10["完整目录页"]
  B10 -- "章节行" --> B7
  B2 -- "换源" --> B9["换源结果页"]
```

### B6 本地书导入状态

```mermaid
flowchart TD
  B6A["书架更多 / 空状态"] --> B6B["请求文件访问权限"]
  B6B -- "拒绝" --> B6C["权限说明与重试"]
  B6B -- "允许" --> B6D["系统文件选择器"]
  B6D -- "选择文件" --> B6E["解析与导入进度"]
  B6E -- "全部成功" --> B6F["成功反馈并返回书架"]
  B6E -- "部分失败" --> B6G["结果清单：成功 / 跳过 / 失败"]
  B6E -- "全部失败" --> B6H["错误说明与重新选择"]
```

## 书籍搜索

```mermaid
flowchart TD
  C1["书籍搜索首页"] -- "提交搜索" --> C2["书籍搜索结果"]
  C2 -- "结果行" --> C3["书籍信息页"]
  C2 -- "换源/来源" --> C4["换源结果页"]
  C3 -- "加入书架" --> C5["书架"]
  C3 -- "开始阅读" --> C6["阅读页"]
```

## 内容替换

```mermaid
flowchart TD
  D1["阅读页"] -- "替换" --> D2["替换快捷覆盖层"]
  D2 -- "全部规则" --> D3["替换规则页"]
  D2 -- "新增规则" --> D4["替换规则编辑页"]
  D2 -- "规则行" --> D4
  D3 -- "新增规则" --> D4
  D3 -- "规则行" --> D4
  D4 -- "保存规则" --> D3
```

## 朗读

```mermaid
flowchart TD
  E1["阅读控制层"] -- "底栏 朗读" --> E2["朗读快捷模块"]
  E2 -- "开始朗读" --> E3["朗读运行胶囊"]
  E2 -- "语速 / 音色 / 范围 / 定时" --> E4["参数选择页/底表"]
  E2 -- "更多设置" --> E5["朗读设置页"]
  E3 -- "关闭" --> E1
```

## 自动翻页

```mermaid
flowchart TD
  F1["阅读页"] -- "自动翻页" --> F2["自动翻页设置覆盖层"]
  F2 -- "开始自动翻页" --> F3["自动翻页运行胶囊"]
  F2 -- "更多设置" --> F4["自动翻页完整设置页"]
  F3 -- "关闭" --> F1
```

## 书源管理

```mermaid
flowchart TD
  G1["书源管理列表"] --> G2["书源详情"]
  G1 -- "新增" --> G3["新增书源"]
  G1 -- "导入" --> G4["书源导入"]
  G2 -- "编辑" --> G5["编辑书源"]
  G5 -- "测试" --> G6["书源测试结果"]
```

## 设置与同步

```mermaid
flowchart TD
  H1["设置"] --> H2["全局设置"]
  H1 --> H3["阅读设置入口"]
  H1 --> H4["同步设置入口"]
  H1 --> H5["备份设置"]
  H1 --> H6["WebDAV 配置"]
  H1 --> H7["进度同步状态"]
  H1 --> H8["关于版本"]
  H1 --> H9["反馈"]
```
