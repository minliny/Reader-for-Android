# 迁移执行清单

状态：待执行
原则：每批迁移后立即校验，不跨批混改

## 1. 第 0 批：规划冻结

目标：确认新目录结构和迁移规则。

验收：

- `00-总览/00-设计目录.md` 已存在。
- `00-总览/01-页面分级.md` 已存在。
- `00-总览/02-目录迁移映射.md` 已存在。
- `01-全局规范/00-页面包模板.md` 已存在。
- `01-全局规范/01-目录命名规范.md` 已存在。

## 2. 第 1 批：主标签页迁移

状态：已完成
目标：把 `主标签页规划/` 收敛到 `02-主标签页/`。

动作：

1. 迁移四个页面包到：
   - `02-主标签页/书架/`
   - `02-主标签页/发现/`
   - `02-主标签页/RSS/`
   - `02-主标签页/设置/`
2. 图片目录从 `图片/drafts/` 改为每个页面包内的 `图片/`。
3. 删除页面包中的 `drafts` 命名。
4. 更新图片总册条目。
5. 删除空的旧 `主标签页规划/` 目录。

验收：

```bash
python3 scripts/audit_ui_image_text_specs.py
```

并确认：

- 主标签页目录内不出现 `drafts`。
- 页面包文件名全部中文。
- 底部主导航只包含 `书架 / 发现 / RSS / 设置`。

完成记录：

- 当前入口：`docs/ui-design/02-主标签页/`
- 旧目录：`docs/ui-design/主标签页规划/` 已移除。
- 校验：`images=179 catalog_entries=179 failures=0`。

## 3. 第 2 批：全局规范迁移

状态：已完成
目标：把可复用规范从页面目录抽到 `01-全局规范/`。

动作：

1. `02-主标签页/02-应用外壳组件视觉规格.md` 迁移为 `01-全局规范/02-应用外壳组件视觉规格.md`。
2. `书架主Tab页面规划/19-页面设计输入约束示例.md` 曾迁移为 `01-全局规范/03-页面设计输入约束.md`；当前已收敛到 `01-全局规范/00-唯一约束参考.md`。
3. 书架首页专属规格保留在书架页面包或 `03-书架链路/书架首页/`。

验收：

- 页面包只引用全局规范，不复制全局规范内容。
- 引用路径全部更新。

完成记录：

- 当前入口：`docs/ui-design/01-全局规范/02-应用外壳组件视觉规格.md`
- 当前唯一约束入口：`docs/ui-design/01-全局规范/00-唯一约束参考.md`
- 校验：`images=179 catalog_entries=179 failures=0`。

## 4. 第 3 批：书架链路拆分

状态：已完成
目标：把 `书架主Tab页面规划/` 拆成页面包。

页面包：

```text
03-书架链路/
  书架空状态/
  分组管理/
  书籍操作底表/
  排序与筛选/
  本地书导入/
  书籍搜索/
  书籍详情/
```

动作：

1. 每个页面包按模板补齐 `00-页面包说明.md` 至 `06-禁止项.md`。
2. 把相关图片复制到对应页面包 `图片/`。
3. 搜索 loading/empty/error 归入 `书籍搜索/图片/`。
4. 详情 loading/error 归入 `书籍详情/图片/`。
5. 阅读入口转移到 `04-阅读链路/阅读入口/`。

验收：

- 书架链路不再混入阅读控制层完整页面。
- 书籍操作底表仍只包含 `修改` 和 `删除`。
- 图片总册通过。

完成记录：

- 当前入口：`docs/ui-design/03-书架链路/`
- 书架首页根页面：`docs/ui-design/02-主标签页/书架/`
- 阅读入口：`docs/ui-design/04-阅读链路/阅读入口/`
- 旧目录：`docs/ui-design/书架主Tab页面规划/` 已移除。
- 校验：`images=176 catalog_entries=176 failures=0`。

## 5. 第 4 批：阅读链路整理

状态：已完成
目标：把 `drafts/03` 至 `40`、`45`、`46` 等阅读相关材料按页面包归类。

页面包：

```text
04-阅读链路/
  沉浸阅读/
  阅读控制层/
  目录与书签/
  朗读/
  阅读外观/
  阅读设置/
  内容搜索/
  内容替换/
  自动翻页/
  换源/
```

验收：

- 阅读控制层母版和派生模块关系清楚。
- 历史候选、失败图、备份图进入历史归档。
- 手机和平板规格不互相覆盖。

完成记录：

- 当前入口：`docs/ui-design/04-阅读链路/`
- 阅读相关图片已从 `docs/ui-design/91-历史归档/drafts/旧全局草稿目录/` 迁入对应页面包 `图片/` 目录。
- 相关 `specs/` 文字稿已迁入对应阅读页面包。
- 校验：`images=176 catalog_entries=176 failures=0`。

## 6. 第 5 批：设置链路整理

状态：已完成
目标：把设置首页下的二级页面和书源管理放到 `05-设置链路/`。

注意：

- `48` 组人工 FAIL，不作为当前 source。
- `44/47` 书源管理候选图可保留，但必须补页面包文字稿。
- `46` 阅读设置如果作为 App 设置入口，需与阅读器内设置关系写清楚。

完成记录：

- 当前入口：`docs/ui-design/05-设置链路/`
- 书源管理候选图已迁入 `05-设置链路/书源管理/图片/`。
- `48` 组设置二级页已迁入对应页面包，但仍标记为重画参考。
- 阅读设置完整页归入 `04-阅读链路/阅读设置/`。
- 校验：`images=176 catalog_entries=176 failures=0`。

## 7. 第 6 批：审计与历史归档

状态：部分完成
目标：顶层只保留新分区目录，不再混放历史文件。

动作：

1. 图片总册迁到 `90-审计与索引/`。
2. Stitch 映射、上传清单、评审队列迁到 `90-审计与索引/`。
3. 旧 `drafts/`、`planning/`、`demos/` 迁到 `91-历史归档/`。
4. 更新脚本和引用路径。

验收：

- `docs/ui-design/` 顶层不再有大量散落 `.md`。
- 审计脚本通过。
- 新增页面不再写入旧目录。

完成记录：

- `drafts/` 顶层 PNG/SVG 已清空。
- 发现候选/历史图已迁入 `06-发现链路/`。
- RSS 候选图已迁入 `07-RSS链路/`。
- `51` 组全局状态模板已迁入 `91-历史归档/全局状态模板/`。
- 图片总册仍保留在旧位置，后续需要迁移脚本路径后再移动。
- 校验：`images=176 catalog_entries=176 failures=0`。
