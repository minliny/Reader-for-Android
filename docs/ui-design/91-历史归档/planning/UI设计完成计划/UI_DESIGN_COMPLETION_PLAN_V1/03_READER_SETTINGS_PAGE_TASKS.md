# 阅读内设置完整页任务

> 状态更新（2026-06-11）：默认页及子页已生成 `46`～`46-5`。下表“需要设计图”保留为
> 原始任务字段，当前应理解为“已生成，待 spec 整合与人工终审”。

阅读内设置完整页从 `22-2-reader-settings-quick-panel-height-aligned.png` 的 `完整设置 >` 进入，不属于控制层本体。

| 页面 ID | 页面名称 | 入口 | 退出 | 需要设计图 | 必要状态 | 备注 |
| ----- | ---- | -- | -- | ----- | ---- | -- |
| `reader_settings_home` | 阅读设置首页 | 设置快捷模块 `完整设置 >` | 返回阅读控制层或阅读页 | 已生成 `46` | 默认、加载、错误 | 默认图已生成；状态模板复用 51 系列。 |
| `reader_settings_display` | 阅读显示设置 | 阅读设置首页 | 返回设置首页 | 已生成 `46-1` | 默认、预览更新、错误 | 屏幕方向、状态栏、导航栏、信息显示；不重复界面外观设置。 |
| `reader_settings_behavior` | 阅读行为设置 | 阅读设置首页 | 返回设置首页 | 已生成 `46-2` | 默认、开关变更、错误 | 音量键翻页、点击区域、手势、单手模式。 |
| `reader_settings_assist` | 阅读辅助设置 | 阅读设置首页 | 返回设置首页 | 已生成 `46-3` | 默认、权限缺失、错误 | 误触保护、章节加载、缓存优先、失败重试。 |
| `reader_settings_preset` | 阅读预设/恢复默认 | 阅读设置首页右上菜单 | 取消/应用后返回 | 已生成 `46-5` | 默认、确认、成功、失败 | `46-4` 补充进度与信息页；确认/反馈复用 51 系列。 |

## 边界规则

- 阅读完整设置页不保留底部四主按钮。
- 阅读完整设置页不重复右侧亮度滑条。
- 字体、主题、版式、翻页动画归 `界面`，不归阅读设置。
