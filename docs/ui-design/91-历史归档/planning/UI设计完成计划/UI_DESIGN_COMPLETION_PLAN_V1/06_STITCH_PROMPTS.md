# Stitch 提示词

通用前缀适用于所有提示词：

```text
画布尺寸：390 × 884。移动端竖屏。中文阅读 App。遵循现有 docs/ui-design/91-历史归档/drafts/旧全局草稿目录 的视觉风格：柔和纸张背景、暖色卡片、分区式信息块、适合长时间阅读的低刺激视觉密度。不要改变主底栏结构。不要重新发明阅读控制层。需要和已有图片保持同一视觉密度。输出可编辑图层，方便进入 Figma。
```

## 1. 阅读控制层 5 张图复刻/细调

```text
基于当前采用图片 16-5-reader-toc-quick-panel-height-aligned.png、18-3-read-aloud-quick-panel-height-aligned.png、19-1-read-aloud-running-capsule-height-aligned.png、22-2-reader-settings-quick-panel-height-aligned.png、25-4-reader-top-more-menu-height-aligned.png 做复刻或极小细调。画布尺寸：390 × 884。移动端竖屏。中文阅读 App。遵循现有 docs/ui-design/91-历史归档/drafts/旧全局草稿目录 的视觉风格。以 04-reader-controls.png 为唯一位置基准。底部章节进度条位于底部工具栏上方。不得出现贴屏幕底部的细进度条。底部工具栏不得下移。正文层不得重排。所有原有元素、文字、按钮、图标、模块结构、视觉风格保持不变。不要重新发明阅读控制层。输出可编辑图层，方便进入 Figma。
```

## 2. 发现页

```text
设计发现页主页面。画布尺寸：390 × 884。移动端竖屏。中文阅读 App。遵循现有 docs/ui-design/91-历史归档/drafts/旧全局草稿目录 的视觉风格。不要改变主底栏结构，主底栏固定为书架、发现、RSS、设置，当前选中发现。发现内容来自书源和订阅源，必须体现信息源选择和来源内分类切换。当前只规划主页面，不生成次级页面、弹层、loading、empty、error。需要和已有图片保持同一视觉密度。输出可编辑图层，方便进入 Figma。
```

## 3. RSS/订阅首页

```text
设计 RSS/订阅首页主页面。画布尺寸：390 × 884。移动端竖屏。中文阅读 App。遵循现有 docs/ui-design/91-历史归档/drafts/旧全局草稿目录 的视觉风格。主底栏固定为书架、发现、RSS、设置，当前选中 RSS。本项目不规划账户功能，不显示我的或用户头像。页面展示订阅概览、筛选标签、订阅源分组、最新订阅条目。当前只规划主页面，不生成添加订阅、订阅源管理、详情页、loading、empty、error。需要和已有图片保持同一视觉密度。输出可编辑图层，方便进入 Figma。
```

## 4. 设置首页

```text
设计设置首页主页面。画布尺寸：390 × 884。移动端竖屏。中文阅读 App。遵循现有 docs/ui-design/91-历史归档/drafts/旧全局草稿目录 的视觉风格。主底栏固定为书架、发现、RSS、设置，当前选中设置。本项目不规划账户功能，不显示我的、用户头像、登录入口或个人主页。页面是 App 级设置首页，不是阅读控制层设置快捷区，也不是阅读内完整设置页。包含本地状态概览、书源管理、RSS/订阅管理、阅读偏好、缓存管理、同步备份、隐私权限、关于反馈等入口。当前只规划主页面，不生成书源管理、RSS 管理、阅读完整设置、同步备份、关于反馈等次级页面。输出可编辑图层，方便进入 Figma。
```

## 5. 书源管理首页

```text
设计书源管理首页。画布尺寸：390 × 884。移动端竖屏。中文阅读 App。遵循现有 docs/ui-design/91-历史归档/drafts/旧全局草稿目录 的视觉风格。不要改变主底栏结构。页面包含标题栏、搜索、分组筛选、书源列表、启用/禁用开关、检测入口、新增书源按钮。需要默认态、加载态、空状态、错误态。输出可编辑图层，方便进入 Figma。
```

## 6. 书源详情 / 编辑页

```text
设计书源详情和编辑页。画布尺寸：390 × 884。移动端竖屏。中文阅读 App。遵循现有 docs/ui-design/91-历史归档/drafts/旧全局草稿目录 的视觉风格。不要改变主底栏结构。详情页包含名称、地址、分组、启用状态、规则摘要、检测结果、编辑/删除操作。编辑页包含表单、校验错误、保存、检测、取消。需要和已有图片保持同一视觉密度。输出可编辑图层，方便进入 Figma。
```

## 7. 阅读完整设置页

```text
设计阅读完整设置页组：阅读设置首页、阅读显示设置、阅读行为设置、阅读辅助设置、阅读预设/恢复默认。画布尺寸：390 × 884。移动端竖屏。中文阅读 App。遵循现有 docs/ui-design/91-历史归档/drafts/旧全局草稿目录 的视觉风格。不要重新发明阅读控制层。完整设置页从设置快捷模块进入，但不保留底部四主按钮和右侧亮度条。需要和已有图片保持同一视觉密度。输出可编辑图层，方便进入 Figma。
```

## 8. 通用 loading / empty / error 状态模板

```text
设计通用 loading、empty、error、network_error、permission_required、delete_confirm、operation_success、operation_failed 状态模板。画布尺寸：390 × 884。移动端竖屏。中文阅读 App。遵循现有 docs/ui-design/91-历史归档/drafts/旧全局草稿目录 的视觉风格。不要改变主底栏结构。模板需要可复用于书架、发现、搜索、书源管理、设置等页面。输出可编辑图层，方便进入 Figma。
```
