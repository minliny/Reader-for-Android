# Control Layer Base V2 Visual Layer Fix Report

## 1. 总体结论

CONTROL_LAYER_BASE_V2_VISUAL_LAYER_FIX_NOT_READY

说明：本轮已通过 Stitch MCP 尝试对目标 screen 做视觉分层精修，并再次发起精确颜色 token 补丁。导出 HTML 核验显示，青蓝强调色 `#366179` 已生效，但底栏、浮动页内控制条、圆形快捷按钮、正文文字等关键层级色仍未完整落到 HTML，因此不能判定为视觉分层修复完成。

## 2. 修复范围

- 原 screen 名称：阅读页控制层基础状态 V2 - 布局精修版
- 原 screen ID：`beca4c724346445d8d55eef996450f95`
- 新 screen 名称：未生成新的持久化 screen；实际仍检查原 screen
- 新 screen ID：`beca4c724346445d8d55eef996450f95`
- 是否修改原 screen：已通过 Stitch MCP 发起编辑，但最终导出 HTML 仅部分颜色生效

## 3. 配色修复表

| 区域 | 修复前颜色 | 修复后颜色 | 是否通过 | 备注 |
|---|---:|---:|---|---|
| 正文背景 | `#fdf6ec` / 暖纸色 | 期望 `#fff8f4` | 否 | Tailwind token 中有 `#fff8f4`，但 body 仍为 `bg-[#fdf6ec]` |
| 底栏 | `#eae1da` | 期望 `#e9ded6` | 否 | footer 仍使用 `bg-surface-container-highest`，token 仍为 `#eae1da` |
| 浮动页内控制条 | `#f5ece6` | 期望 `#efe2d8` / `#eadbd0` | 否 | 仍使用 `bg-surface-container`，token 仍为 `#f5ece6` |
| 圆形快捷按钮 | `#efe7e0` | 期望 `#f7ebe1` | 否 | 仍使用 `bg-surface-container-high`，token 仍为 `#efe7e0` |
| 强调色 | `#8b5000` | `#366179` | 是 | `primary` 已变为青蓝系，进度与亮度强调已指向 `#366179` |
| 控制层图标文字 | `#41484c` | 期望 `#3f4d52` | 否 | `on-surface-variant` 仍为 `#41484c`，接近但未按目标色落地 |
| 正文文字 | `#2c2c2c` / `#1f1b17` | 期望 `#53433f` | 否 | 正文容器未明确落到暖灰棕 `#53433f` |

## 4. 视觉层级自检表

| 层级 | 期望 | 实际 | 是否通过 | 备注 |
|---|---|---|---|---|
| 正文纸面层 | 背景 `#fff8f4`，正文 `#53433f` | token 有 `#fff8f4`，body 与正文色未完全落地 | 否 | 纸面与正文文字体系仍不够稳定 |
| 顶部信息层 | 与正文轻微分离，不厚重 | 保持现有轻分离 | 是 | 顶部结构未回归 |
| 圆形快捷按钮层 | `#f7ebe1`、轻 shadow、细边界、从纸面浮出 | 仍为旧 `#efe7e0` 体系 | 否 | 与正文纸面仍偏近 |
| 浮动页内控制条层 | `#efe2d8` / `#eadbd0`，进度 `#366179` | 进度色已青蓝，容器仍旧暖纸色 | 否 | 浮出感只完成一半 |
| 固定底栏层 | `#e9ded6`，冷墨图标文字，轻分割 | 底栏仍为 `#eae1da` | 否 | 比正文略分离，但未按目标色明确建立层级 |
| 左侧亮度条 | 中层背景、青蓝进度、轻边界阴影 | 强调色已青蓝，容器仍旧色与较重 shadow | 否 | 仍需降低阴影和边界重量 |

## 5. 阴影和边界调整表

| 组件 | 调整内容 | 是否过重 | 是否通过 | 备注 |
|---|---|---|---|---|
| 圆形快捷按钮 | 期望轻 shadow、细描边 | 否 | 否 | 仍为 `shadow-md`，边界与层级色未精确落地 |
| 浮动页内控制条 | 期望轻 shadow、胶囊浮出 | 否 | 部分通过 | 结构和胶囊感保持，颜色未完整生效 |
| 控制层底栏 | 期望轻分割、无强阴影 | 否 | 部分通过 | 未出现强阴影，但目标底栏色未落地 |
| 左侧亮度条 | 期望减轻边框和阴影 | 是 | 否 | 仍为 `shadow-lg`，视觉重量偏高 |
| handoff 虚线边界 | 期望低干扰 | 否 | 是 | 虚线没有压过正式 UI |

## 6. 结构回归自检

| 检查项 | 是否保持 | 是否通过 | 备注 |
|---|---|---|---|
| 顶部三段式 | 是 | 是 | 返回 / 深空信号 / refresh + swap_horiz + more_vert 保持 |
| 顶部第二行 | 是 | 是 | 第一章：阿长与《山海经》 / 本地书籍 chip 保持 |
| 深空信号正文 | 是 | 是 | 未回退到幽谷、深谷或第九章 |
| 快捷按钮无文字标签 | 是 | 是 | 未发现“搜索 / 自动翻页 / 内容替换 / 夜间模式”正式文字标签 |
| 页内控制条 chevron 图标 | 是 | 是 | 保持 `chevron_left` / `chevron_right`，未使用 `skip_previous` / `skip_next` |
| 底栏四项 | 是 | 是 | 目录 / 朗读 / 界面 / 设置 保持 |
| 无弹窗 | 是 | 是 | 未新增弹窗或展开态 |

## 7. 是否仍有 P0

无。

## 8. 是否仍有 P1

1. 视觉层级色未完整落地：底栏、页内控制条、圆形快捷按钮仍使用旧暖纸 token。
2. 正文背景存在 token 与 body 实际 class 不一致：`#fff8f4` 已在 token 中，但 body 仍为 `#fdf6ec`。
3. 正文文字未切换为目标暖灰棕 `#53433f`。
4. 左侧亮度条仍使用较重 `shadow-lg`，浮出感偏重且未按新中层色统一。

## 9. 是否建议再次只读审计

不建议。建议先继续让 Stitch 侧颜色 token 与组件实际样式完全落地，再进入只读审计。
