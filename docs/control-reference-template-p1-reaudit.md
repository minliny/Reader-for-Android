# Control Reference Template P1 Reaudit

## 1. 总体结论

CONTROL_REFERENCE_TEMPLATE_READY_WITH_MINOR_GAPS

## 2. 审计范围

| 类型 | Screen 名称 | Screen ID | 纳入结论 |
| --- | --- | --- | --- |
| 主审计对象 | 阅读页控制层参照模板 - 基于修正版含亮度条 | d0608caa5be247ca94e49ea7241a88d6 | 是 |
| 视觉参考对象 | 阅读页 (控制层展示) - 修正版 (含亮度条) | a41d6cb56ba843cab804568532085fec | 否 |

参考报告：`docs/control-reference-template-audit.md`。

P1 修复报告：`docs/control-reference-template-p1-fix-report.md` 未找到，因此本轮直接以 Stitch 当前 screen 内容为准复审。

## 3. P1 修复复查表

| P1 项 | 期望 | 实际 | 是否通过 | 备注 |
| --- | --- | --- | --- | --- |
| 左侧亮度条底部图标 | 明确的右向圆润箭头，指向屏幕右侧；不再使用 `keyboard_tab_rtl` | 当前仍为 Material Symbols `keyboard_tab_rtl` | 未通过 | 亮度条结构未破坏，但该 P1 未修复 |
| 快捷方式区四等分布局 | 4 项明确各占 25% 或等价四列等宽布局 | 当前容器仍为 `flex justify-between items-center`，入口未声明 `flex-1`、`w-1/4`、`basis-1/4` 或 `grid-cols-4` | 未通过 | 视觉上为 4 项横向分布，但实现约束仍不够明确 |

## 4. 顶部区域回归检查表

| 检查项 | 期望 | 实际 | 是否通过 | 备注 |
| --- | --- | --- | --- | --- |
| 第一行结构 | 返回箭头 / 深空信号 / 刷新图标 / 换源图标 / 更多图标 | `arrow_back` / `深空信号` / `refresh` / `swap_horiz` / `more_vert` | 通过 | 无 P0 回归 |
| 左侧返回 | 返回箭头 | `arrow_back` | 通过 | 符合要求 |
| 中间书名 | 显示「深空信号」 | 显示「深空信号」 | 通过 | 未显示 Reader 作为 UI 标题 |
| 禁止 Reader 标题 | 不显示「Reader」作为标题 | 顶部 UI 未显示 Reader | 通过 | HTML `<title>` 含 Reader Interface，不属于界面标题 |
| 右侧图标按钮 | 3 个图标按钮 | 3 个 `p-2` 图标按钮 | 通过 | 点击区域一致 |
| 刷新图标 | 圆圈箭头图标 | `refresh` | 通过 | 无文字「刷新」 |
| 换源图标 | 横向互换箭头图标 | `swap_horiz` | 通过 | 无文字「换源」 |
| 更多图标 | 竖向三点 | `more_vert` | 通过 | 符合要求 |
| 图标尺寸和点击区域 | 一致 | 三个右侧按钮均使用 `p-2` | 通过 | 视觉一致 |
| 第二行独立性 | 独立占据一整行 | 标题栏下方独立一行 | 通过 | 有边界标注 |
| 章节 | 「第一章：阿长与《山海经》」 | 已显示 | 通过 | 无缺失 |
| 来源 chip | 描边 chip / badge / 胶囊标签「本地书籍」 | `rounded-full border border-outline` | 通过 | 不是普通副标题文字 |
| 禁止「来源：」 | 不出现「来源：」 | 未出现 | 通过 | 无 P0 回归 |

## 5. 正文与亮度条检查表

| 检查项 | 期望 | 实际 | 是否通过 | 备注 |
| --- | --- | --- | --- | --- |
| 阅读背景 | 浅米色阅读背景 | `bg-[#fdf6e3]` | 通过 | 保持阅读页形态 |
| 正文章节标题 | 显示「深空信号」 | 已显示 | 通过 | 未改成卡片或列表 |
| 中文正文 | 多段中文正文 | 已显示多段中文 | 通过 | 正文结构保留 |
| 正文边界 | 有明确边界标注 | `main` 有 `border-2 border-dashed` | 通过 | 可用于 handoff |
| 亮度条位置 | 左侧中部 | `fixed left-2 top-1/2 -translate-y-1/2` | 通过 | 未进入顶部或底部面板 |
| 亮度条形态 | 竖向胶囊 | `rounded-full`、`w-[48px]`、`shadow-lg` | 通过 | 形态保留 |
| 顶部亮度图标 | 亮度 / 自动亮度图标 | `brightness_auto` | 通过 | 图标保留 |
| 竖向 slider | 有轨道、进度和滑块 | 轨道、60% 进度、圆形滑块均存在 | 通过 | 结构完整 |
| 底部图标 | 明确右向圆润箭头 | 仍为 `keyboard_tab_rtl` | 未通过 | P1 未修复，但不构成 P0 |
| 亮度条边界 | 边界标注保留 | 整体和内部元素均有 dashed / outline 边界 | 通过 | 边界清晰 |

## 6. 底部控制面板检查表

| 检查项 | 期望 | 实际 | 是否通过 | 备注 |
| --- | --- | --- | --- | --- |
| 底部面板整体 | 位于底部、覆盖正文、浅色面板、顶部圆角 | `fixed bottom-0`、`bg-surface-container`、`rounded-t-xl` | 通过 | 未改成弹窗、全屏或普通底栏 |
| 三层结构 | 快捷方式 / 章节跳转 / 底栏 | 三层均存在 | 通过 | 无 P0 回归 |
| 快捷方式数量 | 正好 4 项 | 4 项 | 通过 | 无新增或减少 |
| 快捷方式顺序 | 搜索 / 自动翻页 / 内容替换 / 夜间模式 | 顺序正确 | 通过 | 无错误入口 |
| 快捷方式图文结构 | 上图标 + 下文字 | 四项均为图标加文字 | 通过 | 符合要求 |
| 快捷方式四等分 | 明确 25% 或等价四列等宽 | 仍为 `justify-between` 松散布局 | 未通过 | P1 未修复 |
| 快捷方式边界 | 整体和四个入口边界保留 | 均有 dashed / outline 边界 | 通过 | 范围清楚 |
| 章节跳转位置 | 位于快捷区和底栏之间 | 已位于中间层 | 通过 | 无回归 |
| 章节跳转结构 | 上一章 / 进度条 / 下一章 | `skip_previous` / slider / `skip_next` | 通过 | 有拖拽圆点 |
| 章节跳转边界 | 整体边界保留 | 有 dashed 边界和 slider 边界 | 通过 | 清晰 |
| 底栏数量 | 正好 4 项 | 4 项 | 通过 | 无多余入口 |
| 底栏顺序 | 目录 / 朗读 / 界面 / 设置 | 顺序正确 | 通过 | 符合要求 |
| 底栏普通状态 | 无任何选中效果 | 四项均为 `text-on-surface-variant` | 通过 | 「界面」无高亮背景、无蓝色选中、无橙/粉胶囊 |
| 底栏对齐 | 图标中心、文字基线、上下间距一致 | 视觉基本一致 | 通过 | 未见明显错位 |
| 底栏边界 | 整体和四个入口边界保留 | 均有 dashed / outline 边界 | 通过 | 清晰 |

## 7. 边界标注检查表

| 元素 | 是否应有边界 | 实际是否有边界 | 是否通过 | 备注 |
| --- | --- | --- | --- | --- |
| 顶部第一行主标题操作栏 | 是 | 是 | 通过 | `border-b border-dashed border-outline-variant` |
| 顶部第二行章节与来源标识行 | 是 | 是 | 通过 | `border-b border-dashed border-outline-variant` |
| 阅读正文区域 | 是 | 是 | 通过 | `main` 有 dashed 边界 |
| 左侧悬浮亮度条 | 是 | 是 | 通过 | 整体及内部元素边界保留 |
| 底部控制面板整体 | 是 | 是 | 通过 | wrapper 有 dashed 边界 |
| 快捷方式区整体 | 是 | 是 | 通过 | 分区边界清晰 |
| 快捷方式区四个单独入口 | 是 | 是 | 通过 | 四个 button 均有 dashed 边界 |
| 章节快速跳转区整体 | 是 | 是 | 通过 | 分区边界清晰 |
| 底栏功能区整体 | 是 | 是 | 通过 | nav 区域边界保留 |
| 底栏四个单独入口 | 是 | 是 | 通过 | 四个 button 均有 dashed 边界 |

## 8. P0 剩余问题

无。

## 9. P1 剩余问题

1. 左侧亮度条底部图标仍为 `keyboard_tab_rtl`，尚未改成明确的右向圆润箭头。
2. 快捷方式区仍使用 `flex justify-between`，尚未明确为 25% 四等分或等价四列等宽布局。

## 10. 是否允许进入后续弹层派生

允许。

原因：当前 screen 未发现 P0 回归，顶部两行结构、来源 chip、左侧亮度条、底部三层控制面板、章节跳转区、底栏普通状态和主要边界标注均保持正确。但两个既有 P1 仍未修复，因此不能提升为 `CONTROL_REFERENCE_TEMPLATE_READY`，维持 `CONTROL_REFERENCE_TEMPLATE_READY_WITH_MINOR_GAPS`。
